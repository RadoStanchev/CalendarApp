@using CalendarApp.Controllers
@using CalendarApp.Infrastructure.Extentions

@{
    var currentUser = HttpContext.User;
    var isAuthenticated = currentUser?.Identity?.IsAuthenticated ?? false;
}

<ul class="navbar-nav">
@if (isAuthenticated)
{
    <li class="nav-item">
        <a class="nav-link text-dark d-inline-flex align-items-center" asp-area="" asp-controller="@typeof(NotificationsController).GetControllerName()" asp-action="@nameof(NotificationsController.Index)" data-notification-link title="Преглед на известията">
            <span aria-hidden="true">&#128276;</span>
            <span class="badge bg-danger ms-1 d-none" data-notification-count>0</span>
            <span class="visually-hidden">Известия</span>
        </a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="" asp-controller="@typeof(AccountController).GetControllerName()" asp-action="@nameof(AccountController.Profile)">Здравей, @currentUser?.Identity?.Name!</a>
    </li>
    <li class="nav-item">
        <form  class="form-inline" asp-area="" asp-controller="@typeof(AccountController).GetControllerName()" asp-action="@nameof(AccountController.Logout)">
            <button type="submit" class="nav-link btn btn-link text-dark">Изход</button>
        </form>
    </li>
}
else
{
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="" asp-controller="@typeof(AccountController).GetControllerName()" asp-action="@nameof(AccountController.Register)">Регистрация</a>
    </li>
    <li class="nav-item">
        <a class="nav-link text-dark" asp-area="" asp-controller="@typeof(AccountController).GetControllerName()" asp-action="@nameof(AccountController.Login)">Вход</a>
    </li>
}
</ul>
